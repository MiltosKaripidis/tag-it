<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:background="@drawable/selector_touch_effect"
    android:clickable="true"
    android:gravity="center"
    android:paddingLeft="16dp"
    android:paddingRight="16dp">

    <ImageButton
        android:id="@+id/row_create_more_image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@android:color/white"
        android:src="@drawable/selector_row_image_button" />

    <TextView
        android:id="@+id/row_create_title_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/row_create_more_image_view"
        android:text="@string/tag_title"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/row_create_difficulty_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/row_create_title_text_view"
        android:layout_toLeftOf="@id/row_create_more_image_view"
        android:text="HARD"
        android:textSize="22sp" />

</RelativeLayout>